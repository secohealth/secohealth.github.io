language: python
python: 3.4
install:
 - pip install -r requirements.txt
script:
 - python genja.py
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: output/
  target_branch: test
  on:
    branch: sources
