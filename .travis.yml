language: python
python: 3.4
script:
 - sh build.sh
deploy:
  edge:
    branch: v1.8.47
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: output/
  target_branch: master
  on:
    branch: sources
